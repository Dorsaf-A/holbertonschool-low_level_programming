#include "holberton.h"

/**
 * print_last_digit - check the code for Holberton School students.
 * _abs - valeur absolut
 * @n: int
 * Return: 1 or 0 or -1
 */
int print_last_digit(int n)
{
int i;

i = (n % 10) + '0';
if (i >= 0)
return (i);
else if
}
